<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext HTTP to the local gateway during development
  (API_BASE_URL defaults to the emulator's host-loopback alias,
  10.0.2.2, which Android blocks over cleartext by default since API 28).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
