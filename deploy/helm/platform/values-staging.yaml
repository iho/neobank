# Staging — single-instance Postgres + Redis.

cnpg:
  instances: 1
  storageSize: 20Gi
  backup:
    enabled: false

redis:
  replicas: 1
  storageSize: 4Gi

goledger:
  enabled: true

monitoring:
  podMonitor:
    enabled: true

networkPolicy:
  enabled: true