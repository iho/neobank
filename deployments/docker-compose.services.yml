# Application services overlay. Use with the infra stack:
#
#   make up-all        # infra + migrations + all five services
#   make down-all
#
# goledger is still external — start it on the host (see services/ledger/README.md)
# and reach it via host.docker.internal:50051 (Linux: host-gateway mapping below).

x-build-args: &build-args
  GIT_SHA: ${GIT_SHA:-dev}
  BUILD_DATE: ${BUILD_DATE:-unknown}

x-service-env: &service-env
  DATABASE_URL: postgres://neobank:neobank@postgres:5432/neobank?sslmode=disable
  REDIS_URL: redis://redis:6379/0
  JWT_SECRET: dev-secret-change-me
  KAFKA_BROKERS: redpanda:9092
  OTEL_EXPORTER_OTLP_ENDPOINT: otel-collector:4317
  LEDGER_GRPC_ADDR: host.docker.internal:50051

x-http-healthcheck: &http-healthcheck
  interval: 10s
  timeout: 5s
  retries: 8
  start_period: 20s

services:
  vault-init:
    image: hashicorp/vault:1.17
    profiles: ["vault-pii"]
    environment:
      VAULT_ADDR: http://vault:8200
      VAULT_TOKEN: dev-root-token
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        until vault status -address=http://vault:8200 2>/dev/null; do sleep 1; done
        vault secrets enable transit 2>/dev/null || true
        vault write -f transit/keys/pii type=aes256-gcm96
        vault write -f transit/keys/pii-phone type=hmac key_size=256
        echo "vault transit keys ready"
    depends_on:
      vault:
        condition: service_healthy
    restart: "no"

  migrate:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.migrate
    environment:
      DATABASE_URL: postgres://neobank:neobank@postgres:5432/neobank?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  kyc:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.service
      args:
        <<: *build-args
        SERVICE: simulators/kyc
    environment:
      DATABASE_URL: postgres://neobank:neobank@postgres:5432/neobank?sslmode=disable
      HTTP_PORT: "8092"
      USER_SERVICE_EVENTS_URL: http://user:8081/webhooks/kyc/events
      KYC_WEBHOOK_SECRET: dev-kyc-webhook-secret
    ports:
      - "8092:8092"
    healthcheck:
      <<: *http-healthcheck
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8092/health >/dev/null"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    restart: unless-stopped

  user:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.service
      args:
        <<: *build-args
        SERVICE: user
    environment:
      <<: *service-env
      HTTP_PORT: "8081"
      GRPC_PORT: "50052"
      NOTIFICATION_SERVICE_URL: http://notification:8083/api/v1/internal/events
      KYC_VENDOR_SERVICE_URL: http://kyc:8092
      KYC_WEBHOOK_SECRET: dev-kyc-webhook-secret
    ports:
      - "8081:8081"
      - "50052:50052"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      <<: *http-healthcheck
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8081/health >/dev/null"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kyc:
        condition: service_healthy
    restart: unless-stopped

  notification:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.service
      args:
        <<: *build-args
        SERVICE: notification
    environment:
      <<: *service-env
      HTTP_PORT: "8083"
      GRPC_PORT: "50055"
      USER_GRPC_ADDR: user:50052
    ports:
      - "8083:8083"
      - "50055:50055"
    healthcheck:
      <<: *http-healthcheck
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8083/health >/dev/null"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      user:
        condition: service_healthy
    restart: unless-stopped

  rails:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.service
      args:
        <<: *build-args
        SERVICE: simulators/rails
    environment:
      DATABASE_URL: postgres://neobank:neobank@postgres:5432/neobank?sslmode=disable
      HTTP_PORT: "8090"
      PAYMENT_WEBHOOK_URL: http://payment:8082/webhooks/rails
      RAILS_WEBHOOK_SECRET: dev-rails-webhook-secret
    ports:
      - "8090:8090"
    healthcheck:
      <<: *http-healthcheck
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8090/health >/dev/null"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    restart: unless-stopped

  fx:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.service
      args:
        <<: *build-args
        SERVICE: simulators/fx
    environment:
      DATABASE_URL: postgres://neobank:neobank@postgres:5432/neobank?sslmode=disable
      HTTP_PORT: "8093"
    ports:
      - "8093:8093"
    healthcheck:
      <<: *http-healthcheck
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8093/health >/dev/null"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    restart: unless-stopped

  payment:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.service
      args:
        <<: *build-args
        SERVICE: payment
    environment:
      <<: *service-env
      HTTP_PORT: "8082"
      GRPC_PORT: "50053"
      USER_SERVICE_URL: http://user:8081
      USER_GRPC_ADDR: user:50052
      NOTIFICATION_SERVICE_URL: http://notification:8083/api/v1/internal/events
      RAILS_SERVICE_URL: http://rails:8090
      RAILS_WEBHOOK_SECRET: dev-rails-webhook-secret
      FX_SERVICE_URL: http://fx:8093
    ports:
      - "8082:8082"
      - "50053:50053"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      <<: *http-healthcheck
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8082/health >/dev/null"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      user:
        condition: service_healthy
      notification:
        condition: service_healthy
      rails:
        condition: service_healthy
      fx:
        condition: service_healthy
    restart: unless-stopped

  cardproc:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.service
      args:
        <<: *build-args
        SERVICE: simulators/cardproc
    environment:
      DATABASE_URL: postgres://neobank:neobank@postgres:5432/neobank?sslmode=disable
      HTTP_PORT: "8091"
      CARD_SERVICE_AUTHORIZE_URL: http://card:8084/webhooks/cardproc/authorize
      CARD_SERVICE_EVENTS_URL: http://card:8084/webhooks/cardproc/events
      CARDPROC_WEBHOOK_SECRET: dev-cardproc-webhook-secret
    ports:
      - "8091:8091"
    healthcheck:
      <<: *http-healthcheck
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8091/health >/dev/null"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    restart: unless-stopped

  card:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.service
      args:
        <<: *build-args
        SERVICE: card
    environment:
      <<: *service-env
      HTTP_PORT: "8084"
      GRPC_PORT: "50054"
      USER_SERVICE_URL: http://user:8081
      USER_GRPC_ADDR: user:50052
      NOTIFICATION_SERVICE_URL: http://notification:8083/api/v1/internal/events
      CARDPROC_SERVICE_URL: http://cardproc:8091
      CARDPROC_WEBHOOK_SECRET: dev-cardproc-webhook-secret
    ports:
      - "8084:8084"
      - "50054:50054"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      <<: *http-healthcheck
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8084/health >/dev/null"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      user:
        condition: service_healthy
      notification:
        condition: service_healthy
      cardproc:
        condition: service_healthy
    restart: unless-stopped

  gateway:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.service
      args:
        <<: *build-args
        SERVICE: gateway
    environment:
      <<: *service-env
      HTTP_PORT: "8080"
      USER_GRPC_ADDR: user:50052
      PAYMENT_GRPC_ADDR: payment:50053
      CARD_GRPC_ADDR: card:50054
      NOTIFICATION_GRPC_ADDR: notification:50055
      APP_ENV: development
    ports:
      - "8080:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      <<: *http-healthcheck
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/health >/dev/null"]
    depends_on:
      user:
        condition: service_healthy
      payment:
        condition: service_healthy
      card:
        condition: service_healthy
      notification:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped