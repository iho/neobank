// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: preferences.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
)

const getNotificationPreferences = `-- name: GetNotificationPreferences :one
SELECT user_id, transfers, cards, kyc, push, email, updated_at
FROM notification.notification_preferences
WHERE user_id = $1
`

func (q *Queries) GetNotificationPreferences(ctx context.Context, userID uuid.UUID) (NotificationNotificationPreference, error) {
	row := q.db.QueryRow(ctx, getNotificationPreferences, userID)
	var i NotificationNotificationPreference
	err := row.Scan(
		&i.UserID,
		&i.Transfers,
		&i.Cards,
		&i.Kyc,
		&i.Push,
		&i.Email,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertNotificationPreferences = `-- name: UpsertNotificationPreferences :one
INSERT INTO notification.notification_preferences (user_id, transfers, cards, kyc, push, email)
VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT (user_id) DO UPDATE
SET transfers = EXCLUDED.transfers,
    cards = EXCLUDED.cards,
    kyc = EXCLUDED.kyc,
    push = EXCLUDED.push,
    email = EXCLUDED.email,
    updated_at = now()
RETURNING user_id, transfers, cards, kyc, push, email, updated_at
`

type UpsertNotificationPreferencesParams struct {
	UserID    uuid.UUID
	Transfers bool
	Cards     bool
	Kyc       bool
	Push      bool
	Email     bool
}

func (q *Queries) UpsertNotificationPreferences(ctx context.Context, arg UpsertNotificationPreferencesParams) (NotificationNotificationPreference, error) {
	row := q.db.QueryRow(ctx, upsertNotificationPreferences,
		arg.UserID,
		arg.Transfers,
		arg.Cards,
		arg.Kyc,
		arg.Push,
		arg.Email,
	)
	var i NotificationNotificationPreference
	err := row.Scan(
		&i.UserID,
		&i.Transfers,
		&i.Cards,
		&i.Kyc,
		&i.Push,
		&i.Email,
		&i.UpdatedAt,
	)
	return i, err
}
