// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: cards.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
)

const cancelCard = `-- name: CancelCard :exec
UPDATE cardproc.cards SET status = 'cancelled' WHERE id = $1
`

func (q *Queries) CancelCard(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, cancelCard, id)
	return err
}

const createCard = `-- name: CreateCard :one
INSERT INTO cardproc.cards (external_ref, cardholder_name, pan_token, last_four, expiry_month, expiry_year)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, external_ref, cardholder_name, pan_token, last_four, expiry_month, expiry_year, status, created_at
`

type CreateCardParams struct {
	ExternalRef    string
	CardholderName string
	PanToken       string
	LastFour       string
	ExpiryMonth    int16
	ExpiryYear     int16
}

func (q *Queries) CreateCard(ctx context.Context, arg CreateCardParams) (CardprocCard, error) {
	row := q.db.QueryRow(ctx, createCard,
		arg.ExternalRef,
		arg.CardholderName,
		arg.PanToken,
		arg.LastFour,
		arg.ExpiryMonth,
		arg.ExpiryYear,
	)
	var i CardprocCard
	err := row.Scan(
		&i.ID,
		&i.ExternalRef,
		&i.CardholderName,
		&i.PanToken,
		&i.LastFour,
		&i.ExpiryMonth,
		&i.ExpiryYear,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}

const getCardByID = `-- name: GetCardByID :one
SELECT id, external_ref, cardholder_name, pan_token, last_four, expiry_month, expiry_year, status, created_at
FROM cardproc.cards
WHERE id = $1
`

func (q *Queries) GetCardByID(ctx context.Context, id uuid.UUID) (CardprocCard, error) {
	row := q.db.QueryRow(ctx, getCardByID, id)
	var i CardprocCard
	err := row.Scan(
		&i.ID,
		&i.ExternalRef,
		&i.CardholderName,
		&i.PanToken,
		&i.LastFour,
		&i.ExpiryMonth,
		&i.ExpiryYear,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}
