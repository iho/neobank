// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: quotes.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createQuote = `-- name: CreateQuote :one
INSERT INTO fx.quotes (from_currency, to_currency, amount, converted_amount, rate, spread_bps, expires_at)
VALUES ($1, $2, $3::numeric, $4::numeric, $5::numeric, $6, $7)
RETURNING id, from_currency, to_currency, amount::text AS amount, converted_amount::text AS converted_amount,
          rate::text AS rate, spread_bps, status, created_at, expires_at, executed_at
`

type CreateQuoteParams struct {
	FromCurrency    string
	ToCurrency      string
	Amount          pgtype.Numeric
	ConvertedAmount pgtype.Numeric
	Rate            pgtype.Numeric
	SpreadBps       int32
	ExpiresAt       pgtype.Timestamptz
}

type CreateQuoteRow struct {
	ID              uuid.UUID
	FromCurrency    string
	ToCurrency      string
	Amount          string
	ConvertedAmount string
	Rate            string
	SpreadBps       int32
	Status          string
	CreatedAt       pgtype.Timestamptz
	ExpiresAt       pgtype.Timestamptz
	ExecutedAt      pgtype.Timestamptz
}

func (q *Queries) CreateQuote(ctx context.Context, arg CreateQuoteParams) (CreateQuoteRow, error) {
	row := q.db.QueryRow(ctx, createQuote,
		arg.FromCurrency,
		arg.ToCurrency,
		arg.Amount,
		arg.ConvertedAmount,
		arg.Rate,
		arg.SpreadBps,
		arg.ExpiresAt,
	)
	var i CreateQuoteRow
	err := row.Scan(
		&i.ID,
		&i.FromCurrency,
		&i.ToCurrency,
		&i.Amount,
		&i.ConvertedAmount,
		&i.Rate,
		&i.SpreadBps,
		&i.Status,
		&i.CreatedAt,
		&i.ExpiresAt,
		&i.ExecutedAt,
	)
	return i, err
}

const getQuoteByID = `-- name: GetQuoteByID :one
SELECT id, from_currency, to_currency, amount::text AS amount, converted_amount::text AS converted_amount,
       rate::text AS rate, spread_bps, status, created_at, expires_at, executed_at
FROM fx.quotes
WHERE id = $1
`

type GetQuoteByIDRow struct {
	ID              uuid.UUID
	FromCurrency    string
	ToCurrency      string
	Amount          string
	ConvertedAmount string
	Rate            string
	SpreadBps       int32
	Status          string
	CreatedAt       pgtype.Timestamptz
	ExpiresAt       pgtype.Timestamptz
	ExecutedAt      pgtype.Timestamptz
}

func (q *Queries) GetQuoteByID(ctx context.Context, id uuid.UUID) (GetQuoteByIDRow, error) {
	row := q.db.QueryRow(ctx, getQuoteByID, id)
	var i GetQuoteByIDRow
	err := row.Scan(
		&i.ID,
		&i.FromCurrency,
		&i.ToCurrency,
		&i.Amount,
		&i.ConvertedAmount,
		&i.Rate,
		&i.SpreadBps,
		&i.Status,
		&i.CreatedAt,
		&i.ExpiresAt,
		&i.ExecutedAt,
	)
	return i, err
}

const markQuoteExecuted = `-- name: MarkQuoteExecuted :one
UPDATE fx.quotes
SET status = 'executed', executed_at = now()
WHERE id = $1
RETURNING id, from_currency, to_currency, amount::text AS amount, converted_amount::text AS converted_amount,
          rate::text AS rate, spread_bps, status, created_at, expires_at, executed_at
`

type MarkQuoteExecutedRow struct {
	ID              uuid.UUID
	FromCurrency    string
	ToCurrency      string
	Amount          string
	ConvertedAmount string
	Rate            string
	SpreadBps       int32
	Status          string
	CreatedAt       pgtype.Timestamptz
	ExpiresAt       pgtype.Timestamptz
	ExecutedAt      pgtype.Timestamptz
}

func (q *Queries) MarkQuoteExecuted(ctx context.Context, id uuid.UUID) (MarkQuoteExecutedRow, error) {
	row := q.db.QueryRow(ctx, markQuoteExecuted, id)
	var i MarkQuoteExecutedRow
	err := row.Scan(
		&i.ID,
		&i.FromCurrency,
		&i.ToCurrency,
		&i.Amount,
		&i.ConvertedAmount,
		&i.Rate,
		&i.SpreadBps,
		&i.Status,
		&i.CreatedAt,
		&i.ExpiresAt,
		&i.ExecutedAt,
	)
	return i, err
}
