// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: applicants.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createApplicant = `-- name: CreateApplicant :one
INSERT INTO kyc.applicants (external_ref, full_name, date_of_birth, country_code, status, reason)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, external_ref, full_name, date_of_birth, country_code, status, reason, created_at, decided_at
`

type CreateApplicantParams struct {
	ExternalRef string
	FullName    string
	DateOfBirth pgtype.Date
	CountryCode string
	Status      string
	Reason      string
}

func (q *Queries) CreateApplicant(ctx context.Context, arg CreateApplicantParams) (KycApplicant, error) {
	row := q.db.QueryRow(ctx, createApplicant,
		arg.ExternalRef,
		arg.FullName,
		arg.DateOfBirth,
		arg.CountryCode,
		arg.Status,
		arg.Reason,
	)
	var i KycApplicant
	err := row.Scan(
		&i.ID,
		&i.ExternalRef,
		&i.FullName,
		&i.DateOfBirth,
		&i.CountryCode,
		&i.Status,
		&i.Reason,
		&i.CreatedAt,
		&i.DecidedAt,
	)
	return i, err
}

const getApplicantByID = `-- name: GetApplicantByID :one
SELECT id, external_ref, full_name, date_of_birth, country_code, status, reason, created_at, decided_at
FROM kyc.applicants
WHERE id = $1
`

func (q *Queries) GetApplicantByID(ctx context.Context, id uuid.UUID) (KycApplicant, error) {
	row := q.db.QueryRow(ctx, getApplicantByID, id)
	var i KycApplicant
	err := row.Scan(
		&i.ID,
		&i.ExternalRef,
		&i.FullName,
		&i.DateOfBirth,
		&i.CountryCode,
		&i.Status,
		&i.Reason,
		&i.CreatedAt,
		&i.DecidedAt,
	)
	return i, err
}

const resolveApplicant = `-- name: ResolveApplicant :one
UPDATE kyc.applicants
SET status = $1, reason = $2, decided_at = now()
WHERE id = $3
RETURNING id, external_ref, full_name, date_of_birth, country_code, status, reason, created_at, decided_at
`

type ResolveApplicantParams struct {
	Status string
	Reason string
	ID     uuid.UUID
}

func (q *Queries) ResolveApplicant(ctx context.Context, arg ResolveApplicantParams) (KycApplicant, error) {
	row := q.db.QueryRow(ctx, resolveApplicant, arg.Status, arg.Reason, arg.ID)
	var i KycApplicant
	err := row.Scan(
		&i.ID,
		&i.ExternalRef,
		&i.FullName,
		&i.DateOfBirth,
		&i.CountryCode,
		&i.Status,
		&i.Reason,
		&i.CreatedAt,
		&i.DecidedAt,
	)
	return i, err
}
